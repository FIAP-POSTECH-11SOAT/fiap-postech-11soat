apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain # 'Retain' is safer for database data
  storageClassName: storageclass-postgres-db
  hostPath:
    path: '/Users/igorgregorio/projects/fiap-postech-11soat/k8s/database/data'
    type: DirectoryOrCreate
